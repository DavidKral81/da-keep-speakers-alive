# Da Keep Speakers Alive - Windows app
# Tested on Python 3.14.4 (Windows 11).
#
#   py -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
#   start.bat
#
# tkinter is part of the standard library on Windows, no package needed.

sounddevice==0.5.6    # PortAudio - playback through WASAPI
numpy==2.5.2          # generating the sine burst
pystray==0.19.5       # icon in the notification area
pillow==12.3.0        # icon drawing (also required by pystray)

# Only needed to BUILD the installer, not to run the app. Left commented out
# so a plain "pip install -r requirements.txt" stays small; install it by hand
# when packaging:
#
#   .venv\Scripts\python.exe -m pip install pyinstaller==6.22.2
#
# pyinstaller==6.22.2          # pulls in altgraph and pefile by itself

# The versions above are pinned on purpose. .venv is never committed - it is a
# compiled copy of Python for one machine - so this file is the only record of
# what the app was actually tested against, and an unpinned line would let a
# future "pip install" fetch something that no longer works.
